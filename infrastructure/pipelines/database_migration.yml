pool:
  vmImage: 'ubuntu-latest'

steps:  
- task: AzureKeyVault@1
  inputs:
    azureSubscription: 'Azure subscription 1(ff581936-424c-495e-a9ed-0c247bdd4d78)'
    KeyVaultName: 'fmm-dev'
    SecretsFilter: 'cr-create'
    RunAsPreJob: true
- task: AzureCLI@2
  displayName: "Remove Azure DevOps service connection"
  inputs:
    azureSubscription: "$(SUBSCRIPTION_NAME)"
    addSpnToEnvironment: true
    scriptType: "bash"
    scriptLocation: "inlineScript"
    inlineScript: |
      echo $(cr-create) | az devops service-endpoint list